import React from 'react';
import './Product.css';

const Product = (props) => {
    const{  title, img, instructor, rating, duration, student, price} = props.Product;
    return (
        <div>
            
        </div>
    );
};

export default Product;